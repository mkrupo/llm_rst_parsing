You are an expert annotator. Based on the definitions, analyze the target text (tsv-formatted) and build a bracketed discourse tree end-to-end. Refer to text fragments by their index. Ensure the correct formatting of the tree.

```text
PRECHECK:
Apply the omission test to determine whether the candidate relation is mononuclear
or multinuclear.

If one span can be omitted while the other still performs the same local text function,
    mark the indispensable span as NUCLEUS
    and the omissible/supporting span as SATELLITE.

If both spans remain independently functional and equally central,
    treat the relation as MULTINUCLEAR.

Use relation definitions and the intended reader effect to validate nuclearity.
```

```text
If the omission test indicates that neither span is more nuclear, then
    if the spans describe states of affairs in temporal or procedural order
        assign sequence
    else if exactly two spans are comparable and differ in writer-relevant respects
        assign contrast
    else if the spans enumerate comparable information of the same type
        assign list
    else
        assign joint
```

```text
Else, if the omission test identifies NUCLEUS and SATELLITE,
test the intended reader effect and prefer the most specific applicable relation.
```

```text
If S is acknowledged as true or plausible although it conflicts with N, then
    assign concession
else if S is disfavored or rejected and recognizing the incompatibility increases regard for N
    assign antithesis
```

```text
Else if S supports an assertion in N, then
    if S increases R's belief that N is true
        assign evidence
    else if S supports W's right, basis, or authority to present N
        assign justify
```

```text
Else if S evaluates or frames the significance of N, then
    if S appraises N on an evaluative scale
        assign evaluation
    else if S relates N to a broader conceptual framework
        assign interpretation
```

```text
Else if N is an action to be performed by R, then
    if S increases R's desire to perform N
        assign motivation
    else if S increases R's ability to perform N
        assign enablement
```

```text
Else if the relation involves hypothetical, future, or unrealized situations, then
    if S is the intended goal realized through the activity in N
        assign purpose
    else if N depends on the non-realization of S
        assign otherwise
    else if realization of N depends on realization of S
        assign condition
```

```text
Else if the relation is causal, then
    if S causes an intentional action in N
        assign volitional cause
    else if S is an intentional response or result caused by N
        assign volitional result
    else if S non-deliberately causes the situation in N
        assign non-volitional cause
    else if S is a non-deliberate result caused by N
        assign non-volitional result
```

```text
Else if S supplies information that helps R understand or situate N, then
    if S supplies substantive prior or general knowledge required to comprehend N
        assign background
    else if S supplies the temporal, spatial, situational, or interpretive framework for N
        assign circumstance
```

```text
Else if S provides detail about N or an element of N, then
    assign elaboration
```

```text
Else if S presents a problem, question, request, or need and N contributes a resolution, then
    assign solutionhood
```

```text
Else if the relation is mainly textual or organizational, then
    if S readies or orients R toward following N
        assign preparation
    else if S repeats N in different wording with roughly equal scope
        assign restatement
    else if S condenses a larger N into a shorter form
        assign summary
```

```text
Else
    if both spans are equally important
        assign joint
    else
        choose the most informative applicable relation rather than a vague one.
```

bracketed tree example:
```
(SN-Background
        (text 1)
        (NS-evaluation
          (NS-volitional cause (text 2) (NN-list (text 3) (text 4)))
          (NS-evidence (text 5) (text 6))))
```

```tsv
```
